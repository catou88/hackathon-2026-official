# 提交要求修复记录

日期：2026-09-17。本轮对核查中的可在本地完成的文件与代码问题进行了修复；未请求在线模型。

后续在线测试已获用户授权：发现并修复 provider 将 JSON 放入 `message.reasoning` 而 content 为空的兼容问题；52 项测试通过，row 0 真实模型复测通过、partial 0.5，与离线一致。另确认 SDK 超时未能保证整例硬截止，仍待修复。下文保留本地阶段记录，最新状态见根目录 REPORT.md 和 eval/results/live_smoke.json。

最新补充：硬超时及中断resume已修复，80项程序测试通过；离线/在线各70例已完成，在线最长54.032秒，但partial从离线0.252286下降到0.188。最新结果以根目录REPORT.md和eval/results/full70_hard_timeout.json为准；下列旧52/49项及待修超时表述是历史阶段记录。

## 已完成

- 正式三参数命令默认 routed；保留显式 offline 和 single。支持 --agent，提供 `mini_rca.adapter.solve(instruction,dataset_dir,ctx)` 接口。
- 先判 provider error，再处理 choices；同模型最多三次、0.5/1 秒递增等待，超出剩余预算即停止；routed 再尝试同家族备用。
- 节点/容器有明确作用域，派生服务名不进入答案候选，原因类型与作用域匹配。指标组件发现失败可从 trace/log 恢复。
- 逐行解析与失败隔离；单例证据写入失败不阻断后续案例，记录 io_errors。无有效时间或任何可用实体时仍诚实记录失败，不编造答案。
- 配置拒绝超过赛规的时间/费用上限；resume 延续原始总时钟（含停机时间）和全部已落盘尝试费用，包含尚未确认请求的保守预留。
- 新增 Dockerfile、白名单 .dockerignore、英文 README/REPORT、英文四分钟演示脚本、Makefile、评测/验证与独立打包脚本。eval/results 保留可随仓库交付的小型历史及新实验结果。
- 本地凭据已按用户最新消息更新，只存工作区 .env；没有复制到源码或提交包。

## 已验证

- 49 项本地测试通过；含模拟模型路径、失败退避、服务错误正文、组件作用域、单例隔离、恢复预算和适配接口。
- 未修改的官方 validate_submission.py 通过两例，0 警告。设置 VAL_AGENT=mini_rca.adapter；子进程移除凭据，因此验证的是路由入口在缺密钥时的回退输出结构，不是在线模型。
- 7 个真实窗口重复 3 次，共 21/21 完成。每轮 strict 2/7、partial 0.452857、coverage 1；费用 $0。
- 整轮耗时 36.438、37.344、40.937 秒；每例完整流程均值 5.46281 秒，三轮均值的样本标准差 0.33993 秒。
- 当前重复实验未启用经验层；旧在线 A/B 启用了经验层，两者分开记录，不混称为当前在线受控对照。
- 独立提交包包含 43 个文件、一个 Dockerfile；清单哈希一致，检查未发现本地 API 密钥、原始数据或运行日志。导出的代码另行完成两例离线运行，输出契约通过，费用 $0。

## 尚未完成的外部验证

- 本机无 Docker CLI：镜像构建、2 CPU/8 GB、只读数据、端点网络限制、20 案例总耗时和峰值内存未实测。
- 修复后真实 routed/single 的准确率、模型采用率、成本与重复波动尚未验证。
- 公开仓库发布、团队信息、英文演示录制及提交表单未执行；DEMO.md 是讲稿，不是录像。

代码与材料可以继续用于后续验证；这些未完成项不能用单元测试或 Dockerfile 的存在替代。当前英文交付说明以根目录 README.md 和 REPORT.md 为准；旧 REQUIREMENTS_AUDIT.md 保留为修复前快照。
